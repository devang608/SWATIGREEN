
  # SwatiGreens Website Design

  This is a code bundle for SwatiGreens Website Design. The original project is available at(http://localhost:3001/)

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
